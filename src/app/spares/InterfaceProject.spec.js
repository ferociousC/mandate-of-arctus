import InterfaceComponentnni from './spares/InterfaceComponentnni.vue';

describe('InterfaceComponentnni', () => {
  it('should be a InterfaceComponentnni', () => {
    expect(InterfaceComponentnni.name).toEqual('InterfaceComponentnni');
  });
});
